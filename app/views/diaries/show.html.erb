<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @diary.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @diary.content %>
</p>

<p>
  <strong>Comment:</strong>
</p>
</p >
  <% @diary.comments.each do |comment| %>
    <%= comment.comment %> |
    <%= comment.created_at %>
    <br />
  <% end %>
</p>

<%= render @diary.comments.build %>

<%= link_to 'Edit', edit_diary_path(@diary) %> |
<%= link_to 'Back', diaries_path %>
